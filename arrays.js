// arrays

let ninjas = ['shaun', 'ryu', 'chun-li'];

 ninjas[1] = 'ken';
 console.log(ninjas[1]);

 let ages = [20, 25, 30, 35];
 console.log(ages[2]);

 let random = ['shaun', 'crystal', 30, 20];

 // array length
 console.log(ninjas.length);

 // array methods

  ninjas.join(',');
  ninjas.indexOf('chun-li');
  ninjas.concat(['ken', 'crystal']);
 ninjas.push('ken');
 ninjas.pop();

console.log(result);
console.log(ninjas);
